parameters:
  owobjdir: ' '

steps:
    - task: PublishPipelineArtifact@1
      displayName: Save Artifact rel-${{ parameters.owobjdir }}
      inputs: 
        artifactName: rel-${{ parameters.owobjdir }}
        targetPath: rel
