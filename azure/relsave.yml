parameters:
  relVersion: '$(owobjdir)'

steps:
    - task: PublishPipelineArtifact@1
      displayName: Save Artifact rel-${{ parameters.relVersion }}
      inputs: 
        artifactName: rel-${{ parameters.relVersion }}
        targetPath: rel
