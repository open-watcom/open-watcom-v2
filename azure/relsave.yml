parameters:
  relVersion: '$(owobjdir)'

steps:
    - task: PublishPipelineArtifact@1
      displayName: ${{ "Save Artifact rel-parameters.relVersion" }}
      inputs: 
        artifactName: ${{ parameters.relVersion }}
        targetPath: rel
