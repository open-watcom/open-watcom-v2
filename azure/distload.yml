parameters:
  relVersion: '$(owobjdir)'

steps:
    - task: DownloadPipelineArtifact@2
      displayName: Load Artifact distrib-${{ parameters.relVersion }}
      inputs: 
        buildType: 'current'
        artifactName: distrib-${{ parameters.relVersion }}
        targetPath: distrib/ow/bin

