parameters:
  relVersion: '$(owobjdir)'

steps:
    - task: DownloadPipelineArtifact@2
      displayName: "Load Release rel-${{ parameters.relVersion }}"
      inputs: {buildType: 'current', artifactName: rel-${{ parameters.relVersion }}, targetPath: rel}

