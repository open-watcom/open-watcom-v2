parameters:
  owobjdir: ${{ variables.owobjdir }}

steps:
    - task: PublishPipelineArtifact@1
      displayName: Save Artifact distrib-${{ parameters.owobjdir }}
      inputs: 
        artifactName: distrib-${{ parameters.owobjdir }}
        targetPath: distrib/ow/bin
