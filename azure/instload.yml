parameters:
  owobjdir: ${{ variables.owobjdir }}

steps:
    - task: DownloadPipelineArtifact@2
      displayName: Load Artifacts bld-setupgui-${{ parameters.owobjdir }}
      inputs: 
        buildType: 'current'
        artifactName: bld-setupgui-${{ parameters.owobjdir }}
        targetPath: bld/setupgui
        pattern: '*/*setup.exe'
