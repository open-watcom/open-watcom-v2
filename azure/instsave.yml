parameters:
  relVersion: '$(owobjdir)'

steps:
    - task: PublishPipelineArtifact@1
      displayName: Save Artifacts bld-setupgui-${{ parameters.relVersion }}
      inputs: 
        artifactName: bld-setupgui-${{ parameters.relVersion }}
        targetPath: bld/setupgui
