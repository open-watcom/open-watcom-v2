and is started
with the :HP2.&gml.value:eHP2. tag.
Device functions are then entered after the &gml.value tag, and
may be specified on more than one line.
The :HP2.&gml.evalue:eHP2. tag delimits the end of a value section,
and must be the first non-space characters in the line.
