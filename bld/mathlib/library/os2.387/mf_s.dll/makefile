#pmake: build flat 32bit stack inline os2 iX86 dynamic os_os2 cpu_386

processor  = 387
model      = mf
subdir     = mf_s
system     = os2
convention = stack
linkage    = dynamic

!include $(mathlib_dir)/mrglib.mif
