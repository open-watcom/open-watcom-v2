#pmake: build flat 32bit register inline winnt iX86 dynamic os_nt cpu_386

processor  = 387
model      = mf
subdir     = mf_r
system     = winnt
convention = register
linkage    = dynamic
export_only=1

!include $(mathlib_dir)/mrglib.mif
