#pmake: build flat 32bit stack inline winnt iX86 dynamic os_nt cpu_386

processor  = 387
model      = mf
subdir     = mf_s
system     = winnt
convention = stack
linkage    = dynamic
export_only=1

!include $(mathlib_dir)/mrglib.mif
