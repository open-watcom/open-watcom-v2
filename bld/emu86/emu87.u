%C  [-f] &
Options:
 -f     Force the emulator to install itself, even if floating point
        hardware is present on the machine.
Note:
%C  will terminate if it detects the presence of floating point hardware
and -f is not specified.
