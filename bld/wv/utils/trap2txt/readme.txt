trap2txt takes the output from a trap debugging session (use /tdebug=file.txt) to record what was sent between
the client (debugger) and the trap (possibly remote).

It is very simple and ugly but it does most of the job. It will be extended to break out the supplemental
trap calls soon.
