#pmake: build debug flat register linux merge static os_linux cpu_386

processor  = 386
convention = register
model      = mf
system     = linux
linkage    = static
subdir     = mf_rd

clib_release = 0

!include $(clib_dir)/mrglib.mif
