#pmake: build debug flat register threading conio static os_linux cpu_386

processor  = 386
convention = register
model      = mf
system     = linux
linkage    = static

clib_release = 0

!include ../../../objects.mif
!include $(clib_dir)/master.mif
!include $(clib_dir)/mklib.mif
