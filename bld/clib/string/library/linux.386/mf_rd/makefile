#pmake: build debug flat register threading string static os_linux cpu_386

processor  = 386
convention = register
model      = mf
system     = linux
linkage    = static

clib_release = 0

!include $(clib_dir)/mklib.mif
!include ../../../deps.mif
