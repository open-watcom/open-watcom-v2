#pmake: build small stack 32bit generic cgsupp iX86 static os_dos os_qnx os_os2 os_nt os_nov cpu_386
#pmake: msdos qnx os2 winnt netware 
#
#   just a note that (m)odel (s)mall _ (s)tatic (s)tack? is used by netware
#

processor  = 386
convention = stack
model      = ms
system     = generic
linkage    = static

!include ../../../objects.mif
!include $(clib_dir)/master.mif
!include $(clib_dir)/mklib.mif
